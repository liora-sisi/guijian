# 更新记录

## 0.6.3

- 将档案柜的“本次导出格式”从齿轮设置中移到主界面，打开归笺即可看见；
- 逐房和全部最新房间继续共用当前所选格式，避免点击导出后意外得到默认 JSON。

## 0.6.2

- 聊天页小面板直接加入 JSON、Markdown、TXT 格式选择；
- “按所选格式导出本房”会在下载完成后显示实际格式，不再把小面板限定为 JSON 快捷入口。

## 0.6.1

- 聊天页小面板改为自动读取扩展清单中的真实版本号，不再手工写死版本文字；
- 新增发布一致性测试，防止清单、程序包、README 和页面版本再次走散。

## 0.6.0

- 本地档案柜新增 JSON、Markdown、纯文本 TXT 三种下载格式；
- JSON 继续保留原始层、规范化层、校验指纹和完整性证据，供程序与 Echo 使用；
- Markdown 与 TXT 面向直接阅读，逐条区分网页对话时间与首次抓取时间，并保留思考内容；
- 支持逐房和全部最新房间按所选格式导出，格式偏好只保存在扩展本地界面。

## 0.5.0

- 正式命名为“归笺”，加入 LIORA × ELION 品牌、图标和更自然的界面语言；
- DeepSeek 通用结构可在本地学习并跨刷新复用；
- 手动认领消息区后立即接管随页续记；
- 使用明确思考区和已有角色属性作为锚点，谨慎补全交替角色；
- 规范化层分离思考内容和控件噪声，原始层保持不变；
- 旧快照可按原始外壳原位升级，不产生角色变更导致的重复消息；
- 长房虚拟列表相邻修复、历史到顶证据继承和短窗口重复保护；
- 本地档案柜、搜索、逐房/全部导出和二次确认删除。

## 0.1.0 - 0.4.3

内部原型阶段：完成 Kimi、ChatGPT、DeepSeek 的自动续记、回溯补档、检查点、不可覆盖快照、顺序合并、缺口证据和时间证据模型。
