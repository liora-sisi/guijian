# 安全说明

## 设计边界

- 不读取或保存登录凭据；
- 不调用平台隐藏接口；
- 不执行平台写操作；
- 不联网发送对话；
- 不自动枚举或打开其他房间；
- 只有用户点击后才会启动回溯滚动；
- 原始层不可被规范化层覆盖；
- 无顺序证据的窗口保留为分段和缺口，不伪造连续性。

## 报告问题

请通过 GitHub Issues 报告可公开复现的问题。报告中只能使用合成数据或彻底脱敏的最小样本，不要上传真实对话、导出包、Cookie、令牌、验证码、账号信息或完整页面源码。

安全问题请先提交一条不含利用细节和私密数据的 Issue，注明需要私下联系维护者。

## 已知限制

- “页面稳定到顶”不能证明平台服务器端绝对完整；
- 平台页面结构变化可能导致识别失效；
- 实验性通用平台首次可能需要手动认领一条消息；
- 缺少平台原始时间时，只能提供观察时间上界；
- 折叠且不在 DOM 中的内容无法凭空恢复。
